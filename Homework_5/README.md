# Homework 5

## Requirements

Make sure you have the following Python packages installed:
- `matplotlib`
- `numpy`
- `ipython`

You can install them using py pip install, which should be upgraded as well.

## How to run the code:
Run the following command:

```
python3 homework5.py 
```